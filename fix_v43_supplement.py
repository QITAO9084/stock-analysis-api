import re

# 读取当前v4.3文件内容
with open(r"C:\Users\Administrator\WorkBuddy\automation-20260426141732\工程合同风控专家提示词_v4.3.md", "r", encoding="utf-8") as f:
    content = f.read()

# 修正1：替换7.3自动校验程序的旧内容为新的5项校验
old_check = r"""**7.3 自动校验程序**（智能体必须在输出报告前执行，不得跳过，全部校验通过后方可输出报告，否则重新检查）
必须执行以下4项校验，任何一项不通过都不得输出报告。

**校验1：评分一致性校验**
- 读取报告头部"综合风险评分"数值
- 读取评分校验模块"最终得分"数值
- 两者必须完全一致，否则重新计算并修正头部评分

**校验2：风险数量一致性校验**
- 读取"风险速判"中的 N_红/N_橙/N_黄/N_绿 数量
- 读取评分校验模块中的 N_红/N_橙/N_黄/N_绿 数量
- 两者必须完全一致，否则重新统计

**校验3：风险条款展开数量校验**
- 统计"风险条款展开"中实际展开的【红】【橙】【黄】【绿】条款数量
- 与评分校验模块中的 N_红/N_橙/N_黄/N_绿 对比
- 必须完全匹配，否则补充缺失的条款展开。

**校验4：十大专项汇总表完整性校验**
- 检查10个专项是否全部填写，无留空
- 不适用的专项必须填写："不适用 | - | [原因]"
- 任何专项留空都不得输出报告

**校验不通过的处理**：
- 重新执行第6步和第7步，修正所有不一致
- 修正后再次执行4项校验，直到全部通过
- 严禁在校验不通过时输出报告"""

new_check = r"""**7.3 自动校验程序**（智能体必须在输出报告前执行，不得跳过，全部校验通过后方可输出报告，否则重新检查）
必须执行以下5项校验，每项都要明确给出「通过/不通过」结论，全部通过方可输出：
1. **风险数量一致性校验**：风险速判中的「X个极高致命/X个高危/X个中危/X个低危」必须与后面展开的【红】【橙】【黄】【绿】条款数量逐一核对，完全一致方可通过，不一致则重新清点数量
2. **评分计算正确性校验**：严格按照「总扣分=（红数×20）+（橙数×10）+（黄数×5）+（绿数×2），最终得分=MAX(0,100-总扣分)」的规则计算，最终得分必须与「基础信息」模块中的「综合风险评分」完全一致，计算错误则重新计算
3. **扣字与风险对应校验**：扣字识别结果中列出的模糊表述、沉默条款、但书陷阱等数量，必须与风险速判中的对应等级数量匹配（例如9处模糊表述对应9条黄，1处沉默条款对应1条橙，以此类推），不匹配则补充对应风险条款
4. **法律依据正确性校验**：所有引用的法规名称、条款号必须准确，重点核查《工程造价咨询企业管理办法》《民法典》《保障中小企业款项支付条例》《城乡规划法》等常用法规，不得出现文件名错误、条款号错误的问题
5. **输出完整性校验**：所有模块必须完整输出，不得中断，评分校验模块必须包含总扣分计算、最终得分、对应等级，【黄】中危隐患必须列出所有条款，不得遗漏，不完整则补充完整
**校验不通过的处理**：
- 重新执行第6步和第7步，修正所有不一致和遗漏
- 修正后再次执行5项校验，直到全部通过
- 严禁在校验不通过时输出报告"""

content = content.replace(old_check, new_check)

# 修正2：确认【黄】中危隐患提示已明确要求列出所有条款
old_yellow = r"### 【黄】中危隐患提示"
new_yellow = r"""### 【黄】中危隐患提示
> **强制要求**：必须列出所有识别到的中危条款，不得遗漏，数量必须与风险速判中的「X个中危」完全一致，每条必须包含：原文位置、原文摘录、【黄】行业落地后果、风险深度解读、修改替换范本5个部分，不得省略"""
content = content.replace(old_yellow, new_yellow)

# 修正3：确认输出模板与格式要求已增加输出完整性强制要求
old_output = r"## 【输出模板与格式要求（v4.1统一版）】"
new_output = r"""## 【输出模板与格式要求（v4.1统一版）】
> **输出完整性强制要求**：所有模块必须完整输出，不得中断，若单模块内容过长，可分多次输出，但不得省略任何内容，评分校验、【黄】中危隐患、风险条款展开模块不得出现未完结的情况"""
content = content.replace(old_output, new_output)

# 修正4：更新报告模板中的评分校验部分，要求完整输出计算步骤
old_report_check = r"### 评分校验（强制执行，不得省略）"
new_report_check = r"### 评分校验（强制执行，不得省略，必须完整输出所有计算步骤）"
content = content.replace(old_report_check, new_report_check)

# 写回文件
with open(r"C:\Users\Administrator\WorkBuddy\automation-20260426141732\工程合同风控专家提示词_v4.3.md", "w", encoding="utf-8") as f:
    f.write(content)

print("✅ v4.3补充修正完成，所有改动已生效")
